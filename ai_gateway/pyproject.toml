[project]
name = "ai-gateway"
version = "0.1.0"
dependencies = [
  "fastapi>=0.111.0",
  "uvicorn>=0.32.0",
  "numpy>=1.26.0",
]
requires-python = ">=3.11"

[tool.black]
line-length = 100

[tool.ruff]
line-length = 100

